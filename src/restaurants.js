const redirectBtn = document.getElementById('restaurants');

redirectBtn.addEventListener('click', () => {
    // window.location.href changes the current URL of the browser
    window.location.href = 'restaurant-skeleton.html';
});